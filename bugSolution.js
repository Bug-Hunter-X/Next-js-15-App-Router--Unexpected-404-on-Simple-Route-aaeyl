```javascript
// Remove the pages directory or add a rewrite rule in next.config.js to use it.
//The issue was the existence of both a `pages` and `app` directory.  Next.js defaults to the `app` directory; it will ignore the `pages` directory unless explicitly told otherwise.
```